{{!Billing index view}}
<h1 class="display-5">{{title}}</h1>
{{#if user}}
<a class="btn btn-primary" href="/billing/add">Add New Code</a>
{{/if}}

<div id="langDiv">
<table class="table" id="langTable">
  <thead>
    <th>Code</th>
    <th>Description</th>
    <th>Insurance</th>
    <th>Actions</th>
  </thead>
  <tbody>
  {{#each dataset}}
    <tr>
      <td>{{this.code}}</td>
      <td>{{this.description}}</td>
      <td>{{this.insurance}}</td>
      {{#if ../user}}
        <td>
          <a
            class="btn btn-danger"
            href="/billing/delete/{{this._id}}"
            onclick="return confirmDeletion()"
          >Delete</a>
        </td>
        {{/if}}
    </tr>
  {{/each}}
  </tbody>
</table>
</div>
